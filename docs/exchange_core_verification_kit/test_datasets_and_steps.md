# Exchange-Core 测试数据集合与执行步骤

本文档提供完整的测试数据集合定义和执行步骤，用于性能对比分析。

---

## ⚠️ 重要：测试数据生成机制

### 数据来源

**该项目不提供预先保存的固定测试数据文件。**

所有测试数据都是通过**固定 Seed 的伪随机数生成器**在运行时动态生成的。

### Seed 使用策略

| 测试类型 | Seed 值 | 说明 |
|----------|---------|------|
| **吞吐量测试** | `seed = 1` | 固定，多次迭代复用相同数据 |
| **延迟测试** | `seed = 1` | 固定，多次迭代复用相同数据 |
| **持久化测试** | `seed = iteration` | 每次迭代 (0, 1, 2...) 使用不同 seed |
| **日志测试** | `seed = iteration` | 每次迭代使用不同 seed |

### 随机数生成器初始化

```java
// TestOrdersGeneratorSession.java
```

**Java `Objects.hash()` 算法**:
```java
// 等价于
int hash = 1;
hash = 31 * hash + (symbol * -177277);
hash = 31 * hash + (seed * 10037 + 198267);
return hash;
```

### Rust 复现关键点

要在 Rust 中生成**完全相同**的测试数据，必须实现以下 Java 核心算法：

1. **Java `Random` 算法 (Linear Congruential Generator)**
   - 算法公式: `next_seed = (seed * 0x5DEECE66D + 0xB) & ((1 << 48) - 1)`
   - 必须精确实现 `nextInt(bound)` 和 `nextDouble()` 方法的位操作逻辑。
   - 参考标准 JDK 8 源码实现。

2. **Java `Objects.hash()` 算法**
   - 算法公式: `result = 31 * result + element`
   - 初始 result = 1
   - 用于组合 symbol 和 seed 生成最终的 Random 种子。

### 简化对比方案

如果不需要**精确的逐字节相同**的测试数据，而只追求**统计特性一致**（推荐方案），可以：

1. **使用标准的伪随机数生成器** (如 XorShift 或 PCG)
2. **使用相同的参数配置** (用户数、订单数、交易对数等)
3. **使用相同的分布规则** (Pareto 分布、订单类型比例等)
4. **只对比统计特性** (吞吐量、延迟百分位)

---

## 📦 测试数据集合定义

### 数据集 1: SinglePairMargin (单交易对-保证金)

| 参数 | 值 |
|------|-----|
| 总交易次数 | 3,000,000 |
| 订单簿目标深度 | 1,000 单 |
| 用户数 | 2,000 |
| 交易对数 | 1 |
| 交易对类型 | 期货合约 (EUR/USD) |
| 货币 | EUR (978), USD (840) |

**交易对规格 (EUR/USD)**:
- Symbol ID: 5991
- 类型: FUTURES_CONTRACT
- Base/Quote: EUR/USD
- 基础缩放: 1
- 报价缩放: 1
- 买入保证金: 2200
- 卖出保证金: 3210

---

### 数据集 2: SinglePairExchange (单交易对-现货)

| 参数 | 值 |
|------|-----|
| 总交易次数 | 3,000,000 |
| 订单簿目标深度 | 1,000 单 |
| 用户数 | 2,000 |
| 交易对数 | 1 |
| 交易对类型 | 现货 (ETH/XBT) |
| 货币 | ETH (3928), XBT (3762) |

**交易对规格 (ETH/XBT)**:
- Symbol ID: 9269
- 类型: CURRENCY_EXCHANGE_PAIR
- Base: ETH (1 lot = 100,000 szabo = 0.1 ETH)
- Quote: XBT (1 step = 10 satoshi)
- 基础缩放: 100,000
- 报价缩放: 10

---

### 数据集 3: Medium (中等规模)

| 参数 | 值 |
|------|-----|
| 总交易次数 | 3,000,000 |
| 订单簿目标深度 | 1,000,000 单 |
| 用户数 | 3,300,000 |
| 交易对数 | 10,000 |
| 交易对类型 | 混合 (期货+现货) |
| 货币 | USD, EUR, XBT, ETH, LTC (5种) |

---

### 数据集 4: Large (大规模)

| 参数 | 值 |
|------|-----|
| 总交易次数 | 3,000,000 |
| 订单簿目标深度 | 3,000,000 单 |
| 用户数 | 10,000,000 |
| 交易对数 | 50,000 |
| 交易对类型 | 混合 |
| 货币 | 5种 |

---

### 数据集 5: Huge (超大规模)

| 参数 | 值 |
|------|-----|
| 总交易次数 | 10,000,000 |
| 订单簿目标深度 | 30,000,000 单 |
| 用户数 | 33,000,000 |
| 交易对数 | 100,000 |
| 交易对类型 | 混合 |
| 货币 | 5种 |

---

## 📋 货币代码表

| 货币 | 代码 | 精度单位 |
|------|------|----------|
| USD | 840 | 美分 |
| EUR | 978 | 欧分 |
| XBT (Bitcoin) | 3762 | satoshi (1 BTC = 10^8 satoshi) |
| ETH (Ethereum) | 3928 | szabo (1 ETH = 10^6 szabo) |
| LTC (Litecoin) | 4141 | litoshi |

---

## 🎲 命令生成规则

### 命令类型分布 (基准测试阶段)

| 命令类型 | 比例 | 说明 |
|----------|------|------|
| **GTC 限价单** | ~45% | 订单簿缺深度时增加 |
| **IOC 即时单** | ~35% | 订单簿满时的主要类型 |
| **FOK_BUDGET** | ~1% | IOC 的 1/32 |
| **Cancel 取消** | ~9% | 随机取消现有订单 |
| **Move 移动** | ~8% | 随机移动订单价格 |
| **Reduce 减量** | ~3% | 随机减少订单数量 |

### 订单数量生成

```
size = 1 + rand(0..6) * rand(0..6) * rand(0..6)
范围: 1 到 216
分布: 偏向小数量
```

### 价格生成

```
初始价格 = 10^(3.3 + rand * 1.5 + rand * 1.5)
范围: 约 2000 到 200万

价格波动 = min(price * 0.05, 10000)
价格范围 = [price - 波动*5, price + 波动*5]
```

### 多交易对权重分布

使用 **Pareto 分布** 分配交易对的订单权重：

**精确参数**:
```
库: Apache Commons Math3 - ParetoDistribution
scale (xm) = 0.001
shape (α) = 1.5
```

**Java 代码**:
```java
// TestOrdersGenerator.java

// 生成 N 个样本，然后归一化
double[] raw = new double[size];
for (int i = 0; i < size; i++) {
    raw[i] = paretoDistribution.sample();
}
double sum = Arrays.stream(raw).sum();
double[] weights = Arrays.stream(raw).map(x -> x / sum).toArray();
```

**效果**:
- 少数交易对获得大部分订单 (模拟热门交易对)
- Top 10% 交易对约占 50-80% 订单量
- 底部交易对订单量可能只有几十个

### 用户账户货币分配

使用 **Pareto(1, 1.5)** 分布决定每用户持有的货币数：

```java
// UserCurrencyAccountsGenerator.java

// 每用户账户数 = min(1 + sample, currencies.length)
int accountsToOpen = Math.min(
    1 + (int) paretoDistribution.sample(), 
    currencyCodes.length
);
```

**分布效果** (5种货币时):
| 货币数 | 用户比例 |
|--------|----------|
| 1 种 | ~40% |
| 2 种 | ~25% |
| 3 种 | ~15% |
| 4 种 | ~12% |
| 5 种 | ~8% |

---

## ⚙️ 测试执行步骤

### 步骤 1: 初始化交易所

```
1. 创建交易所实例
2. 配置 RingBuffer 大小
3. 配置匹配引擎数量 (ME)
4. 配置风险引擎数量 (RE)
```

**配置参数表**:

| 测试类型 | RingBuffer | ME | RE |
|----------|------------|-----|-----|
| 延迟测试-单交易对 | 2K | 1 | 1 |
| 吞吐测试-单交易对 | 32K | 1 | 1 |
| 吞吐测试-多交易对 | 默认 | 4 | 2-4 |
| 延迟测试-多交易对 | 32K-64K | 4 | 2 |

---

### 步骤 2: 加载交易对

```
对于每个交易对:
  1. 提交 ADD_SYMBOLS 命令
  2. 等待确认 SUCCESS
```

---

### 步骤 3: 初始化用户账户

```
对于每个用户 (uid = 1 到 N):
  1. 提交 ADD_USER 命令
  2. 为用户添加货币余额:
     - 使用 ADJUST_BALANCE 命令
     - 金额: 10_0000_0000 (每种货币)
  3. 等待确认
```

**用户货币分配规则**:
- 使用 Pareto(1, 1.5) 分布决定每用户持有货币数
- 大部分用户持有 1-2 种货币
- 少数用户持有多种货币

---

### 步骤 4: 预填充订单簿 (Fill Phase)

```
目标: 将订单簿填充到目标深度

循环直到订单簿达到目标深度:
  1. 生成 GTC 限价单
  2. 提交到交易所
  3. 处理成交事件
  4. 更新订单簿状态统计
```

**填充完成条件**:
- Ask 侧订单数 >= target_orders / 2
- Bid 侧订单数 >= target_orders / 2

---

### 步骤 5: 基准测试 (Benchmark Phase)

```
循环 N 次迭代:
  1. 记录开始时间
  2. 提交所有基准测试命令
  3. 等待所有命令处理完成
  4. 记录结束时间
  5. 计算 MT/s = 命令数 / 耗时(ms) / 1000
  6. 验证全局余额为零
  7. (可选) 验证订单簿快照
```

---

### 步骤 6: 收集性能指标

#### 吞吐量指标

```
对于每次迭代:
  - 输出: "{迭代号}. {MT/s} MT/s"
  
最终输出:
  - Average: {平均MT/s} MT/s
```

#### 延迟指标

```
收集百分位:
  - P50 (中位数)
  - P90
  - P95
  - P99
  - P99.9
  - P99.99
  - Max (最坏情况)

输出格式:
  {50%=1.2µs, 90%=2.5µs, 95%=4.1µs, 99%=8.3µs, 99.9%=15µs, 99.99%=120µs, W=1.2ms}
```

---

### 步骤 7: 验证

```
1. 全局余额验证:
   - 所有货币的全局余额总和应为 0
   - 无资金泄漏

2. 订单簿快照验证:
   - 最终订单簿状态与预期一致
   - Ask/Bid 深度符合预期

3. 状态哈希验证 (持久化测试):
   - 恢复后的状态哈希 == 原始状态哈希
```

---

## 📊 完整测试矩阵

### 吞吐量测试

| 测试名称 | 数据集 | 迭代次数 | 预期结果 |
|----------|--------|----------|----------|
| throughput_margin | SinglePairMargin | 50 | 记录 MT/s |
| throughput_exchange | SinglePairExchange | 50 | 记录 MT/s |
| throughput_medium | Medium | 25 | 记录 MT/s |
| throughput_large | Large | 25 | 记录 MT/s |
| throughput_huge | Huge | 25 | 记录 MT/s |

### 延迟测试

| 测试名称 | 数据集 | 目标 TPS | 预热次数 |
|----------|--------|----------|----------|
| latency_margin | SinglePairMargin | 200,000 | 16 |
| latency_exchange | SinglePairExchange | 200,000 | 16 |
| latency_medium | Medium | 200,000 | 8 |
| latency_large | Large | 200,000 | 4 |
| latency_huge | Huge | 200,000 | 2 |

### 持久化测试

| 测试名称 | 数据集 | 迭代次数 | 验证内容 |
|----------|--------|----------|----------|
| persistence_margin | SinglePairMargin | 10 | 状态哈希 |
| persistence_medium | Medium | 25 | 状态哈希 + 性能 |
| persistence_huge | Huge | 25 | 状态哈希 + 性能 |

---

## 📈 输出格式规范

### 吞吐量测试输出

```
0. 1.234 MT/s
1. 1.256 MT/s
2. 1.245 MT/s
...
Average: 1.245 MT/s
```

### 延迟测试输出

```
{50%=1.2µs, 90%=2.5µs, 95%=4.1µs, 99%=8.3µs, 99.9%=15µs, 99.99%=120µs, W=1.2ms}
```

### 命令分布输出

```
GTC:45.23% IOC:35.12% FOKB:1.05% cancel:8.32% move:7.89% reduce:2.39%
```

### 持久化测试输出

```
Snapshot {stateId} created in 1.234s
Original state hash: 0x12345678
Load+start time: 0.567s
Restored state hash: 0x12345678
Original throughput: 1.234 MT/s
Restored throughput: 1.198 MT/s (97.1%)
```

---

## 🔧 硬件要求与基准环境

为了进行公平对比，建议参考原项目的基准测试环境。请注意原项目是在 2010 年代的旧硬件上测试的。

### 原项目基准环境 (Reference)
- **CPU**: Dual Intel® Xeon® X5690 (6 cores, 3.47GHz)
- **CPU 设置**: One socket isolated and tickless, Spectre/Meltdown 补丁禁用
- **OS**: RHEL 7.5, network-latency tuned-adm profile
- **Java**: JDK 8u192 (注意: 新版 Java 8 可能有性能 Bug)

### 推荐测试配置 (Modern)

| 数据集 | 最低 RAM | 推荐 CPU | 磁盘 (持久化测试) |
|--------|----------|----------|-------------------|
| **SinglePair** | 4 GB | 4 核 (3GHz+) | < 1 GB SSD |
| **Medium** | 16 GB | 8 核 (3GHz+) | 10+ GB SSD |
| **Large** | 32 GB | 12 核 (3GHz+) | 20+ GB NVMe |
| **Huge** | 64 GB | 16 核 (3GHz+) | 50+ GB NVMe |

**关键优化建议**:
1. **CPU 隔离**: 使用 `isolcpus` 隔离核心给匹配引擎线程
2. **NUMA 绑定**: 确保内存分配与线程在同一 NUMA 节点
3. **GC 策略**: Java 使用 G1 或 ZGC，Rust 无 GC 但需注意内存分配器 (推荐 jemalloc)

---

## ✅ 测试清单

- [ ] SinglePairMargin 吞吐量测试
- [ ] SinglePairExchange 吞吐量测试
- [ ] SinglePairMargin 延迟测试
- [ ] SinglePairExchange 延迟测试
- [ ] Medium 多交易对吞吐量测试
- [ ] Medium 多交易对延迟测试
- [ ] 持久化恢复测试
- [ ] 日志重放测试
- [ ] 全局余额验证通过
- [ ] 订单簿快照匹配
