log_level: "info"
log_dir: "./logs"
log_file: "prod.log"
use_json: true
rotation: "hourly"
sample_rate: 100
enable_tracing: false

gateway:
  host: "0.0.0.0"
  port: 8080
  queue_size: 50000

# PostgreSQL for account management (Phase 0x0A)
postgres_url: "postgresql://trading:trading123@localhost:5432/trading"
