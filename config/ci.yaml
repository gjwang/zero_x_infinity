log_level: "info"
log_dir: "./logs"
log_file: "ci.log"
use_json: false
rotation: "never"
sample_rate: 10
enable_tracing: false

gateway:
  host: "0.0.0.0"
  port: 8080
  queue_size: 10000

persistence:
  enabled: true
  tdengine_dsn: "taos+ws://root:taosdata@localhost:6041"

# PostgreSQL for account management (Phase 0x0A)
# CI uses standard port 5432
postgres_url: "postgresql://trading:trading123@localhost:5432/exchange_info_db?max_connections=20"
