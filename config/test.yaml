log_level: "info"
log_dir: "./logs"
log_file: "test.log"
use_json: false
rotation: "never"
sample_rate: 10
enable_tracing: true

gateway:
  host: "127.0.0.1"
  port: 8081
  queue_size: 1000

# PostgreSQL for account management (Phase 0x0A)
postgres_url: "postgresql://trading:trading123@localhost:5432/exchange_info_db"
