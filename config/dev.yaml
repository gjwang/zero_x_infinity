log_level: "debug"
log_dir: "./logs"
log_file: "dev.log"
use_json: false
rotation: "daily"
sample_rate: 1
enable_tracing: true

gateway:
  host: "0.0.0.0"
  port: 8080
  queue_size: 10000

persistence:
  enabled: true  # TDengine is running
  tdengine_dsn: "taos+ws://root:taosdata@localhost:6041"

# PostgreSQL for account management (Phase 0x0A)
postgres_url: "postgresql://trading:trading123@localhost:5433/trading"
